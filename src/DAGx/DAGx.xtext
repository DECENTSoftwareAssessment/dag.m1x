// automatically generated by Xtext
grammar DAGx.DAGx hidden(WS)


import "platform:/resource/dag.m1/model/DAG.ecore"
import "http://www.eclipse.org/emf/2002/Ecore" as ecore

Graph returns Graph:
    {Graph}
    (nodes+=Node '\n')*
    ;

EString returns ecore::EString:
    ID;

Node returns Node:
    {Node}
    name=ID
    (parents+=[Node|ID])*
    ;

terminal ID : ('a'..'z'|'A'..'Z'|'_'|'0'..'9')+;
terminal WS : (' ')+;

